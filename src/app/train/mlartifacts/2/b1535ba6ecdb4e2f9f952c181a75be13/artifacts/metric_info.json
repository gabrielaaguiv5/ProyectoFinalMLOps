{
  "accuracy_score-2_X_test": "accuracy_score(y_true=y_test, y_pred=y_test_pred)",
  "accuracy_score_X_train": "accuracy_score(y_true=y_train, y_pred=y_train_pred)",
  "f1_score-2_X_test": "f1_score(y_true=y_test, y_pred=y_test_pred, average='weighted', zero_division=0)",
  "f1_score_X_train": "f1_score(y_true=y_train, y_pred=y_train_pred, average='weighted', zero_division=0)",
  "precision_score-2_X_test": "precision_score(y_true=y_test, y_pred=y_test_pred, average='weighted', zero_division=0)",
  "precision_score_X_train": "precision_score(y_true=y_train, y_pred=y_train_pred, average='weighted', zero_division=0)",
  "recall_score-2_X_test": "recall_score(y_true=y_test, y_pred=y_test_pred, average='weighted', zero_division=0)",
  "recall_score_X_train": "recall_score(y_true=y_train, y_pred=y_train_pred, average='weighted', zero_division=0)"
}